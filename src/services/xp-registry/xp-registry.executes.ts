import { useWalletStore } from '@store/walletStore'
import { NFTId } from './xp-registry.types'

const NEXT_PUBLIC_XP_REGISTRY_CONTRACT =
  process.env.NEXT_PUBLIC_XP_REGISTRY_CONTRACT!

export default {
  // getCosmonStats,
}
